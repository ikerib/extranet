<form method="post" action="{{ path('karpeta_delete', {'id': karpetum.id}) }}" onsubmit="return confirm('Seguru zaude ezabatu nahi duzula?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ karpetum.id) }}">
    <button class="btn btn-danger btn-sm">{{ "app.crud.delete" | trans }}</button>
</form>